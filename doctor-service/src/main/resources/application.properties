spring.application.name=doctor-service
server.port=8083

# MySQL — doctor's OWN database
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_doctor?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=B1sm1lla#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Clean up: Hibernate detects this automatically now
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Data Initialization Fix
spring.sql.init.mode=always

# Logging
logging.config=classpath:log4j2.xml

